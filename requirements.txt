# nanobot 框架依赖（通过 sys.path 引用 nanobot-main 目录）
# nanobot 自身需要: pydantic, pydantic-settings, httpx, loguru 等

# 股票助手业务依赖
pandas>=2.0
matplotlib>=3.8
numpy>=1.24
tabulate>=0.9
statsmodels>=0.14
prophet>=1.1

# Gradio Web 界面
gradio>=4.0,<6.0
fastapi>=0.100.0

# 数据 ETL 依赖
tushare>=1.4.0
openpyxl>=3.1.0
pymysql>=1.1.0
